<%= form_for(api_v1_progress) do |f| %>
  <% if api_v1_progress.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_v1_progress.errors.count, "error") %> prohibited this api_v1_progress from being saved:</h2>

      <ul>
      <% api_v1_progress.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
